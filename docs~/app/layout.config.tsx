import { createBaseOptions } from 'ach-fumadocs-theme';

export const baseOptions = createBaseOptions({
  title: 'Achieve Package Manager',
  githubUrl: 'https://github.com/achieveonepark/achieve-package-manager',
});
